vim.bo.modifiable = false

-- local keymap = require('ty.core.keymap')
-- local n, cmd = keymap.nmap, keymap.cmd
-- n('<leader>q', 'Quit', cmd('Ty.NOTIFY("OK")', { buffer = vim.api.nvim_get_current_buf(), "+group" }))
